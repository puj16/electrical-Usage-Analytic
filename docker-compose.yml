version: "3.9"

services:

  python-analytics:
    build: .

    container_name: python-analytics

    ports:
      - "5000:5000"

    environment:
      DB_HOST: ${DB_HOST}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_PORT: ${DB_PORT}